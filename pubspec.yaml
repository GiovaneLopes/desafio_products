name: desafio_products
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  dartz: ^0.10.1
  mockito: ^5.4.2
  internet_connection_checker: ^1.0.0+1
  dio: ^5.3.0
  google_fonts: ^5.1.0
  auto_size_text: ^3.0.0
  get_it: ^7.6.0
  network_image_mock: ^2.1.1
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.4.6

flutter:
  uses-material-design: true

  assets:
    - assets/images/
